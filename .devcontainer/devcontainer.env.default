test_email_address=TEMP

gitlab_url=TEMP
gitlab_access_token=TEMP
# this is the project the calendar wiki will be posted to
gitlab_calendar_wiki_project_id=TEMP

smtp_address=TEMP
smtp_port=TEMP
smtp_user_name=TEMP
smtp_password=TEMP

DB_PATH=/app/timelord/database/timelord.db

logging=DEBUG

# Allow these email addresses as senders even if not regitered in Gitlab. Seperate with a comma.
# e.g. a@b.com,b@c.com
EXPLICIT_ALLOW_EMAILS = 
